---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: comfy-route
spec:
  parentRefs:
  - name: internal
    namespace: kube-system
    sectionName: https
  hostnames:
  - comfy.${PRIVATE_DOMAIN}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: comfy-backend
      namespace: ai
      port: 80