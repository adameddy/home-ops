---
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: comfyui-route
  namespace: default
spec:
  parentRefs:
  - name: internal
    sectionName: https
  hostnames:
  - comfy.${PRIVATE_DOMAIN}
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /
    backendRefs:
    - name: comfyui-backend
      port: 8188