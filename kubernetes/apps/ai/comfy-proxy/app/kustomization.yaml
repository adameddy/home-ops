---
apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
resources:
  - ./helm-release.yaml
generatorOptions:
  disableNameSuffixHash: true
configMapGenerator:
  - name: comfy-proxy-config
    files:
      - config-map.yaml
